# 1.网络接口层

* 采用muduo网络库

```c++
    //文件传输服务器 3线程
    EventLoop loop;
    InetAddress addr(8080, "10.20.32.132");
    FileServer server(&loop, addr, "file-server");
    server.start();
    loop.loop();
    return 0;
```

* 定义loop;
* 绑定ip地址和端口号addr;
* 把loop和addr传入构造函数中;
* start启动;
* loop循环开始;

