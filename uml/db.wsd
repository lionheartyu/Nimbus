@startuml
title Nimbus 云盘系统 MySQL 用户认证与权限管理流程图

start

:用户注册请求;
:连接 MySQL 数据库;
:生成随机盐，密码加盐哈希;
:插入 users 表;
if (插入成功?) then (是)
  :注册成功;
else
  :返回错误（如用户名已存在）;
endif

:用户登录请求;
:连接 MySQL 数据库;
:查询 users 表，获取密码哈希和盐;
if (用户名存在?) then (是)
  :密码加盐哈希比对;
  if (密码正确?) then (是)
    :生成 token;
    :插入 sessions 表;
    :登录成功，返回 token;
  else
    :返回错误（密码错误）;
  endif
else
  :返回错误（用户名不存在）;
endif

:敏感操作请求（如文件操作）;
:校验 token;
:连接 MySQL 数据库;
:查询 sessions 表，验证 token 是否有效;
if (token 有效?) then (是)
  :允许操作;
else
  :拒绝操作，返回未授权;
endif

:用户登出请求;
:连接 MySQL 数据库;
:删除 sessions 表中的 token;
:登出成功;

stop
@enduml